export enum Color {
    BLACK,
    WHITE,
}

export enum MatchStatus {
    READY,
    STARTED,
    FINISHED,
}

export enum Mode {
    SINGLE_PLAYER,
    MULTIPLAYER,
}

export enum PrivacyLevel {
    PUBLIC,
    PRIVATE,
}

export enum RoomStatus {
    OPEN,
    CLOSED,
}
