export enum RoomStatus {
    OPEN,
    CLOSED
}

export enum Mode {
    SINGLE_PLAYER,
    MULTIPLAYER
}

export enum PrivacyLevel {
    PUBLIC,
    PRIVATE
}

export enum MatchStatus {
    READY,
    STARTED,
    FINISHED
}

export enum Color {
    BLACK,
    WHITE
}