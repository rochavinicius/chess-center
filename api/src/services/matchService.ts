import { MatchModel } from "../models/matchModel";

const addMatch = async (roomModel: MatchModel) => {
    // validate business rules
    // map model to prisma
    // add to db
};

module.exports = { addMatch };
