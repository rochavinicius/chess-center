import { RoomModel } from "../models/roomModel"

const addRoom = async (roomModel: RoomModel) => {
    // validate business rules
    // map model to prisma
    // add to db
}

module.exports = { addRoom }
